============
Installation
============

At the command line::

    $ easy_install svvamp

Or::

    $ pip install svvamp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv svvamp
    $ pip install svvamp
