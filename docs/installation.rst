============
Installation
============

SVVAMP is written in Python 3.4.1. It needs Python 3.

At the command line::

    $ easy_install svvamp

Or::

    $ pip install svvamp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv svvamp
    $ pip install svvamp
